Comments which begin with // are not supported by C89.
In C89, variables must be declared at the beginning of a scope.
It's best to append the letter f to a constant that contains a decimal point which is assigned to a float variable to avoid a warning from the compiler. E.g. num = 24.13f;
